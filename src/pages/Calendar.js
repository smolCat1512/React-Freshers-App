import React from "react"
import '../App.css';

    function Calendar() {
        return (
        <main className="fade-in">
            <h1>Calendar Page</h1>
        </main>
        );
    }

export default Calendar;